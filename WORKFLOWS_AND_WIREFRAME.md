# WebGlow Personal Messenger - Complete Workflows & Wireframe

## Table of Contents
1. [User Workflows](#user-workflows)
2. [Technical Workflows](#technical-workflows)
3. [Wireframe & UI Structure](#wireframe--ui-structure)
4. [Technology Stack](#technology-stack)
5. [Data Flow Diagrams](#data-flow-diagrams)

---

## User Workflows

### Workflow 1: Client Registration & First Message

```
START
  ↓
User visits app → Sign Up Page
  ↓
Enter email (e.g., john@example.com)
Enter password, name, phone
  ↓
Click "Sign Up"
  ↓
Firebase creates account
System detects role = "client" (no "admin" in email)
User profile saved to Firestore
  ↓
Redirect to Client Chat View
  ↓
Client sees empty chat with admin
  ↓
Client types message → Click send / Upload file
  ↓
System creates conversation (conv_{clientId})
Message sent to Firestore
assignedAdminId set to first admin who responds
  ↓
Client sees message in chat bubble
  ↓
END
```

### Workflow 2: Admin Registration & Managing Conversations

```
START
  ↓
User visits app → Sign Up Page
  ↓
Enter email with "admin" (e.g., admin@company.com)
Enter password, name, phone
  ↓
Click "Sign Up"
  ↓
Firebase creates account
System detects role = "admin" (contains "admin")
User profile saved to Firestore
  ↓
Redirect to Admin Panel
  ↓
Admin sees list of all client conversations
  ↓
Admin clicks on client conversation
  ↓
Conversation opens in right panel
Admin reads messages (system marks as read)
assignedAdminId automatically set to this admin
  ↓
Admin types response → Click send / Upload file
  ↓
Message sent to Firestore with admin's Firebase UID
  ↓
Client sees admin response in real-time
  ↓
END
```

### Workflow 3: Real-time Messaging with Typing Indicators

```
Client View:
User typing → "input" event fires
  ↓
System writes to /typing/{conversationId}
  { isTyping: true, typerId: clientId }
  ↓
Admin sees "Client is typing..." indicator
  ↓
User stops typing (1.5s debounce)
  ↓
System updates typing status to false
  ↓
Indicator disappears on admin side

---

Admin View (mirror process):
Admin typing → updates typing collection
  ↓
System writes typerId = adminId
  ↓
Client sees "Admin is typing..." indicator
  ↓
Admin sends message or stops typing
  ↓
Indicator disappears on client side
```

### Workflow 4: Media File Upload

```
START
  ↓
User clicks paperclip icon
  ↓
File picker opens
  ↓
User selects image/video/document
  ↓
Frontend validates file
  ↓
POST /api/upload with FormData
  ↓
Server receives file via Multer
  ↓
Server uploads to GitHub using Octokit
  - Path: media/{timestamp}_{filename}
  - Commit message generated
  ↓
GitHub returns download_url
  ↓
Server returns { url, filename, size } to frontend
  ↓
Frontend sends message with:
  - mediaUrl
  - mediaType (image/video/file)
  - fileName
  - fileSize
  ↓
Message appears in chat with media preview
  ↓
END
```

---

## Technical Workflows

### Authentication Flow

```
┌─────────────────────────────────────────────────────────┐
│                    User Opens App                        │
└────────────────────┬────────────────────────────────────┘
                     ↓
            ┌────────────────────┐
            │ AuthProvider loads │
            │ onAuthStateChanged │
            └────────┬───────────┘
                     ↓
         ┌───────────────────────┐
         │ User authenticated?   │
         └───┬───────────────┬───┘
             │               │
            YES             NO
             │               │
             ↓               ↓
    ┌────────────────┐  ┌──────────┐
    │ Fetch user doc │  │ Show     │
    │ from Firestore │  │ AuthPage │
    └────┬───────────┘  └──────────┘
         │
         ↓
    ┌─────────────────┐
    │ Check user.role │
    └────┬────────┬───┘
         │        │
      client    admin
         │        │
         ↓        ↓
  ┌──────────┐ ┌────────────┐
  │ Client   │ │ Admin      │
  │ ChatView │ │ Panel      │
  └──────────┘ └────────────┘
```

### Message Sending Flow

```
┌──────────────────────────────────────────────────────────┐
│              User Types & Clicks Send                     │
└────────────────────┬─────────────────────────────────────┘
                     ↓
            ┌────────────────────┐
            │ Typing indicator   │
            │ activates (1.5s)   │
            └────────┬───────────┘
                     ↓
         ┌───────────────────────┐
         │ Get/Create            │
         │ Conversation          │
         └────────┬──────────────┘
                  ↓
         ┌────────────────────────────────┐
         │ addDoc to messages subcoll     │
         │ {                              │
         │   senderId: currentUser.uid,   │
         │   text: message,               │
         │   timestamp: serverTimestamp() │
         │ }                              │
         └────────┬───────────────────────┘
                  ↓
         ┌────────────────────────────┐
         │ Update conversation doc    │
         │ lastMessage, timestamp     │
         │ increment unreadCount      │
         └────────┬───────────────────┘
                  ↓
         ┌────────────────────────────┐
         │ onSnapshot triggers        │
         │ on all subscribed clients  │
         └────────┬───────────────────┘
                  ↓
         ┌────────────────────────────┐
         │ Message appears in UI      │
         │ with animation             │
         └────────────────────────────┘
```

### Real-time Subscription Flow

```
Component Mounts
      ↓
┌─────────────────────────────────┐
│ subscribeToConversations()      │
│ (Admin only)                    │
└──────────┬──────────────────────┘
           ↓
    ┌──────────────────────────┐
    │ onSnapshot listener      │
    │ on /conversations        │
    └──────┬───────────────────┘
           ↓
    ┌──────────────────────────┐
    │ Update state on changes  │
    └──────────────────────────┘

Component Mounts
      ↓
┌─────────────────────────────────┐
│ subscribeToMessages()           │
│ (Both client & admin)           │
└──────────┬──────────────────────┘
           ↓
    ┌──────────────────────────────┐
    │ onSnapshot on messages       │
    │ subcollection                │
    └──────┬───────────────────────┘
           ↓
    ┌──────────────────────────────┐
    │ Update messages[] state      │
    │ Auto-scroll to bottom        │
    └──────────────────────────────┘

Component Mounts
      ↓
┌─────────────────────────────────┐
│ subscribeToTypingStatus()       │
│ (Both client & admin)           │
└──────────┬──────────────────────┘
           ↓
    ┌──────────────────────────────┐
    │ onSnapshot on typing doc     │
    │ /typing/{conversationId}     │
    └──────┬───────────────────────┘
           ↓
    ┌──────────────────────────────┐
    │ Show/hide typing indicator   │
    └──────────────────────────────┘

Component Unmounts
      ↓
┌─────────────────────────────────┐
│ Cleanup: unsubscribe()          │
│ Prevents memory leaks           │
└─────────────────────────────────┘
```

---

## Wireframe & UI Structure

### Client Chat View Layout

```
┌─────────────────────────────────────────────────────────────┐
│  ┌─────┐  WebGlow Messenger               [Profile] [⋮]    │
│  │ W  │                                                     │
│  └─────┘                                                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   Chat with Admin                                           │
│   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │
│                                                             │
│   ┌─────────────────────────────────────┐                  │
│   │ Hello, I need help with my order    │  [You]           │
│   │ 10:30 AM                    ✓✓      │                  │
│   └─────────────────────────────────────┘                  │
│                                                             │
│           ┌─────────────────────────────────────┐          │
│   [Admin] │ Sure! Let me check that for you.    │          │
│           │ 10:32 AM                            │          │
│           └─────────────────────────────────────┘          │
│                                                             │
│   ┌──────────────────┐                                     │
│   │ [Image Preview]  │  [You]                              │
│   │                  │                                      │
│   │ Screenshot.png   │                                      │
│   │ 10:35 AM    ✓✓   │                                      │
│   └──────────────────┘                                     │
│                                                             │
│   [Admin is typing...]                                      │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│  [📎]  [Type a message...]                        [Send]   │
└─────────────────────────────────────────────────────────────┘
```

### Admin Panel Layout

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  ┌─────┐  WebGlow Admin Panel                         [Profile] [Logout]   │
│  │ W  │                                                                     │
│  └─────┘                                                                     │
├────────────────────────┬────────────────────────────────────────────────────┤
│ Conversations (3)      │  Chat with John Doe                                │
│                        │  john@example.com                                  │
│ ┌──────────────────┐  │  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│ │ 🔵 John Doe      │◀─┤                                                     │
│ │ john@example.com │  │     ┌─────────────────────────────────────┐        │
│ │ "I need help..." │  │     │ I need help with my order           │        │
│ │ 2 mins ago    (2)│  │     │ 10:30 AM                    ✓✓      │        │
│ └──────────────────┘  │     └─────────────────────────────────────┘        │
│                        │                                                     │
│ ┌──────────────────┐  │ ┌─────────────────────────────────────┐            │
│ │   Sarah Smith    │  │ │ Sure! Let me check that for you.    │  [You]     │
│ │ sarah@test.com   │  │ │ 10:32 AM                        ✓✓  │            │
│ │ "Thanks!"        │  │ └─────────────────────────────────────┘            │
│ │ 1 hour ago       │  │                                                     │
│ └──────────────────┘  │     ┌──────────────────┐                           │
│                        │     │ [Image Preview]  │                           │
│ ┌──────────────────┐  │     │                  │                           │
│ │   Mike Johnson   │  │     │ Screenshot.png   │                           │
│ │ mike@company.com │  │     │ 10:35 AM    ✓✓   │                           │
│ │ "All set!"       │  │     └──────────────────┘                           │
│ │ Yesterday        │  │                                                     │
│ └──────────────────┘  │                                                     │
│                        │                                                     │
├────────────────────────┼────────────────────────────────────────────────────┤
│                        │  [📎]  [Type a message...]            [Send]       │
└────────────────────────┴────────────────────────────────────────────────────┘
```

### Authentication Page Layout

```
┌───────────────────────────────────────────────┐
│                                               │
│                                               │
│              ┌─────┐                          │
│              │ W  │  WebGlow Logo             │
│              └─────┘                          │
│                                               │
│         Welcome Back                          │
│   Sign in to continue to WebGlow Messenger    │
│                                               │
│   ┌─────────────────────────────────────┐    │
│   │ Email                               │    │
│   │ you@example.com                     │    │
│   └─────────────────────────────────────┘    │
│                                               │
│   ┌─────────────────────────────────────┐    │
│   │ Password                            │    │
│   │ ••••••••                            │    │
│   └─────────────────────────────────────┘    │
│                                               │
│   ┌─────────────────────────────────────┐    │
│   │          Sign In                    │    │
│   └─────────────────────────────────────┘    │
│                                               │
│   Don't have an account? Sign up              │
│                                               │
└───────────────────────────────────────────────┘
```

---

## Technology Stack

### Frontend Stack

```
┌─────────────────────────────────────────────────┐
│              React 18 + TypeScript              │
├─────────────────────────────────────────────────┤
│                                                 │
│  UI Framework:                                  │
│  ├─ React 18.3.1 (Component library)           │
│  ├─ TypeScript (Type safety)                   │
│  └─ Wouter (Client-side routing)               │
│                                                 │
│  Styling:                                       │
│  ├─ Tailwind CSS (Utility-first)               │
│  ├─ shadcn/ui (Component system)               │
│  ├─ Radix UI (Accessible primitives)           │
│  └─ Framer Motion (Animations)                 │
│                                                 │
│  State Management:                              │
│  ├─ TanStack Query (Server state)              │
│  ├─ React Context (Auth state)                 │
│  └─ React Hooks (Local state)                  │
│                                                 │
│  Form Handling:                                 │
│  ├─ React Hook Form                            │
│  └─ Zod (Schema validation)                    │
│                                                 │
└─────────────────────────────────────────────────┘
```

### Backend Stack

```
┌─────────────────────────────────────────────────┐
│           Node.js + Express + TypeScript        │
├─────────────────────────────────────────────────┤
│                                                 │
│  Server Framework:                              │
│  ├─ Express.js (HTTP server)                   │
│  ├─ TypeScript (Type safety)                   │
│  └─ TSX (TypeScript execution)                 │
│                                                 │
│  Middleware:                                    │
│  ├─ Multer (File upload handling)              │
│  ├─ CORS (Cross-origin requests)               │
│  └─ Express JSON (Body parsing)                │
│                                                 │
│  Build Tools:                                   │
│  ├─ Vite (Dev server + bundler)                │
│  ├─ esbuild (Fast compilation)                 │
│  └─ PostCSS (CSS processing)                   │
│                                                 │
└─────────────────────────────────────────────────┘
```

### Database & Storage

```
┌─────────────────────────────────────────────────┐
│            Firebase + GitHub Storage            │
├─────────────────────────────────────────────────┤
│                                                 │
│  Firebase Services:                             │
│  ├─ Firebase Auth (User authentication)        │
│  ├─ Firestore (NoSQL database)                 │
│  │   ├─ users collection                       │
│  │   ├─ conversations collection               │
│  │   ├─ messages subcollection                 │
│  │   └─ typing collection                      │
│  └─ Real-time listeners (Live updates)         │
│                                                 │
│  Media Storage:                                 │
│  ├─ GitHub Repository (via Octokit)            │
│  ├─ REST API file uploads                      │
│  └─ Public download URLs                       │
│                                                 │
│  Future (Configured):                           │
│  └─ PostgreSQL (Neon serverless)               │
│      └─ Drizzle ORM                            │
│                                                 │
└─────────────────────────────────────────────────┘
```

---

## Data Flow Diagrams

### Complete Message Flow

```
┌─────────────┐
│   Client    │
│  Browser    │
└──────┬──────┘
       │
       │ 1. User types message
       ↓
┌──────────────────┐
│  React Component │
│  (MessageInput)  │
└──────┬───────────┘
       │
       │ 2. sendMessage()
       ↓
┌──────────────────┐
│   messaging.ts   │
│  (Messaging API) │
└──────┬───────────┘
       │
       │ 3. addDoc()
       ↓
┌──────────────────┐
│    Firestore     │
│  /conversations/ │
│  {convId}/       │
│  messages/       │
└──────┬───────────┘
       │
       │ 4. Real-time update
       ↓
┌──────────────────────────────┐
│  onSnapshot() Listeners      │
│  (All subscribed clients)    │
└──────┬───────────────────────┘
       │
       │ 5. State update
       ↓
┌──────────────────┐
│  React State     │
│  messages[]      │
└──────┬───────────┘
       │
       │ 6. Re-render
       ↓
┌──────────────────┐
│   UI Update      │
│  (ChatBubble)    │
└──────────────────┘
```

### File Upload Flow

```
┌─────────────┐
│   Client    │
│  Browser    │
└──────┬──────┘
       │
       │ 1. User selects file
       ↓
┌──────────────────┐
│  React Component │
│  (MessageInput)  │
└──────┬───────────┘
       │
       │ 2. FormData POST
       ↓
┌──────────────────┐
│  Express Server  │
│  /api/upload     │
└──────┬───────────┘
       │
       │ 3. Multer parses
       ↓
┌──────────────────┐
│  Upload Handler  │
│  (routes.ts)     │
└──────┬───────────┘
       │
       │ 4. Octokit upload
       ↓
┌──────────────────┐
│  GitHub API      │
│  Create file     │
└──────┬───────────┘
       │
       │ 5. Returns URL
       ↓
┌──────────────────┐
│  Express Server  │
│  Response JSON   │
└──────┬───────────┘
       │
       │ 6. Send message with URL
       ↓
┌──────────────────┐
│    Firestore     │
│  Message doc     │
│  + mediaUrl      │
└──────────────────┘
```

### Authentication Flow Detail

```
┌─────────────────┐
│  User submits   │
│  credentials    │
└────────┬────────┘
         │
         ↓
┌─────────────────────────────┐
│  signInWithEmailAndPassword │
│  (Firebase Auth SDK)        │
└────────┬────────────────────┘
         │
         ↓
┌─────────────────────────────┐
│  Firebase validates         │
│  Returns user token         │
└────────┬────────────────────┘
         │
         ↓
┌─────────────────────────────┐
│  onAuthStateChanged fires   │
│  AuthProvider updates       │
└────────┬────────────────────┘
         │
         ↓
┌─────────────────────────────┐
│  Fetch user doc from        │
│  /users/{uid}               │
└────────┬────────────────────┘
         │
         ↓
┌─────────────────────────────┐
│  Check user.role field      │
└────────┬────────┬───────────┘
         │        │
    "client"   "admin"
         │        │
         ↓        ↓
┌──────────┐  ┌──────────┐
│ Client   │  │ Admin    │
│ ChatView │  │ Panel    │
└──────────┘  └──────────┘
```

---

## Security Architecture

### Firestore Security Rules Flow

```
┌─────────────────────────────────────────────────┐
│         Client Attempts Operation               │
└────────────────┬────────────────────────────────┘
                 ↓
        ┌────────────────────┐
        │ Firebase validates │
        │ authentication     │
        └────────┬───────────┘
                 ↓
        ┌────────────────────┐
        │ Check security     │
        │ rules              │
        └────┬───────────┬───┘
             │           │
           ALLOW       DENY
             │           │
             ↓           ↓
    ┌────────────┐  ┌─────────┐
    │ Operation  │  │ Error   │
    │ succeeds   │  │ thrown  │
    └────────────┘  └─────────┘

Rules Enforced:
1. Users can only read their own profile
2. Clients can only access their conversation
3. Admins can access all conversations
4. senderId must match authenticated user
5. Typing status validates user identity
```

### Role-Based Access Control

```
┌────────────────────────────────────┐
│    User signs up with email        │
└────────────┬───────────────────────┘
             ↓
      ┌──────────────────┐
      │ Email contains   │
      │ "admin"?         │
      └──┬───────────┬───┘
         │           │
        YES         NO
         │           │
         ↓           ↓
    role="admin"  role="client"
         │           │
         ↓           ↓
┌────────────────┐ ┌──────────────────┐
│ Access:        │ │ Access:          │
│ - All convos   │ │ - Own convo only │
│ - All messages │ │ - Own messages   │
│ - Can respond  │ │ - Can send       │
│ - Admin panel  │ │ - Client view    │
└────────────────┘ └──────────────────┘
```

---

## Performance Optimizations

### Real-time Listener Strategy

```
Component Lifecycle:
┌──────────────────────────────────────┐
│ useEffect(() => {                    │
│   const unsubscribe =                │
│     subscribeToMessages(convId)      │
│                                      │
│   return () => unsubscribe()         │
│ }, [conversationId])                 │
└──────────────────────────────────────┘

Benefits:
- Single listener per component
- Auto-cleanup on unmount
- Prevents memory leaks
- Dependency array updates subscription
```

### Query Optimization

```
Firestore Queries:
1. Conversations: orderBy('lastMessageTimestamp', 'desc')
   → Shows most recent first
   
2. Messages: orderBy('timestamp', 'asc')
   → Chronological order
   
3. Limit queries where appropriate
   → Prevents fetching entire collections

Indexes Required:
- conversations: lastMessageTimestamp (desc)
- messages: timestamp (asc)
```

---

## Deployment Architecture

### Development Environment (Current)

```
┌─────────────────────────────────────┐
│         Replit Container            │
│  ┌───────────────────────────────┐ │
│  │  Vite Dev Server (Port 5000)  │ │
│  │  - HMR enabled                │ │
│  │  - Hot reload                 │ │
│  │  - Source maps                │ │
│  └───────────────────────────────┘ │
│  ┌───────────────────────────────┐ │
│  │  Express Server               │ │
│  │  - Integrated with Vite       │ │
│  │  - /api/upload endpoint       │ │
│  └───────────────────────────────┘ │
└─────────────────────────────────────┘
          ↓
    Proxied through
    Replit's infrastructure
          ↓
┌─────────────────────────────────────┐
│    https://[repl-name].replit.dev   │
└─────────────────────────────────────┘
```

### Production Environment (Autoscale)

```
┌─────────────────────────────────────┐
│      Replit Deployment              │
│  ┌───────────────────────────────┐ │
│  │  Build Process:               │ │
│  │  npm run build                │ │
│  │  - Vite bundles to dist/      │ │
│  │  - TypeScript compiled        │ │
│  │  - Assets optimized           │ │
│  └───────────────────────────────┘ │
│  ┌───────────────────────────────┐ │
│  │  Production Server:           │ │
│  │  - Serves static files        │ │
│  │  - /api/upload active         │ │
│  │  - Auto-scaling enabled       │ │
│  └───────────────────────────────┘ │
└─────────────────────────────────────┘
```

---

## Environment Configuration

### Required Environment Variables

```
┌─────────────────────────────────────────────┐
│ Firebase Configuration (VITE_ prefix)      │
├─────────────────────────────────────────────┤
│ VITE_FIREBASE_API_KEY                      │
│ VITE_FIREBASE_AUTH_DOMAIN                  │
│ VITE_FIREBASE_PROJECT_ID                   │
│ VITE_FIREBASE_STORAGE_BUCKET               │
│ VITE_FIREBASE_MESSAGING_SENDER_ID          │
│ VITE_FIREBASE_APP_ID                       │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ GitHub Configuration (Server-side)         │
├─────────────────────────────────────────────┤
│ GITHUB_PAT                                 │
│ GITHUB_REPO_OWNER                          │
│ GITHUB_REPO_NAME                           │
└─────────────────────────────────────────────┘

Note: VITE_ prefix makes variables available
in client-side code via import.meta.env
```

---

## Future Enhancements

### Planned Features

```
1. Progressive Web App (PWA)
   ├─ Service Worker
   ├─ Offline support
   ├─ Add to Home Screen
   └─ App manifest

2. Push Notifications
   ├─ Firebase Cloud Messaging
   ├─ Background notifications
   └─ Message previews

3. Advanced Features
   ├─ Voice messages
   ├─ Video calls
   ├─ Message reactions
   ├─ Message search
   └─ Conversation archiving

4. Admin Features
   ├─ Canned responses
   ├─ Analytics dashboard
   ├─ User management
   └─ Bulk messaging
```

---

## Component Architecture

### Key Components Hierarchy

```
App
├── AuthProvider (Context)
│   └── Router
│       ├── AuthPage
│       │   ├── SignIn
│       │   └── SignUp
│       │
│       ├── ClientChatView (if role=client)
│       │   ├── ChatHeader
│       │   ├── MessageList
│       │   │   └── ChatBubble (multiple)
│       │   ├── TypingIndicator
│       │   └── MessageInput
│       │       ├── FileUpload
│       │       └── SendButton
│       │
│       └── AdminPanel (if role=admin)
│           ├── ConversationsList
│           │   └── ConversationCard (multiple)
│           │
│           └── ChatWindow
│               ├── ChatHeader
│               ├── MessageList
│               │   └── ChatBubble (multiple)
│               ├── TypingIndicator
│               └── MessageInput
│                   ├── FileUpload
│                   └── SendButton
```

---

## API Endpoints

### REST Endpoints

```
POST /api/upload
├─ Purpose: Upload media files to GitHub
├─ Content-Type: multipart/form-data
├─ Body: { file: File }
├─ Response: { 
│    url: string,
│    filename: string,
│    size: number
│  }
└─ Authentication: Required (session-based)
```

### Firebase SDK Methods (Client-side)

```
Authentication:
- signInWithEmailAndPassword(email, password)
- createUserWithEmailAndPassword(email, password)
- signOut()
- onAuthStateChanged(callback)

Firestore Queries:
- getDoc(doc(db, 'users', uid))
- addDoc(collection(db, 'conversations/.../messages'))
- updateDoc(doc(db, 'conversations', id))
- onSnapshot(query, callback)
- getDocs(query)

Firestore Mutations:
- setDoc(doc(db, 'users', uid), data)
- updateDoc(doc(db, 'conversations', id), data)
- increment(1) - for unread counts
- serverTimestamp() - for timestamps
```

---

**End of Documentation**

This comprehensive workflow and wireframe document covers all aspects of the WebGlow Personal Messenger application, from user interactions to technical implementation details.
